additionalProperties:
  formFields:
    - default: "23333"
      envKey: PANEL_APP_PORT_HTTP
      type: number
      label: 
        en: Port
        zh: 服务端口
      required: true
      rule: paramPort    # 强制端口校验 [^4]

    - default: "2"
      envKey: WARP_SLEEP
      label: 
        en: Start delay
        zh: 启动延迟(秒)
      required: true
      type: number
