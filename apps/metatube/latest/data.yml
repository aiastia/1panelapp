additionalProperties:
  formFields:
    - envKey: PANEL_APP_PORT_HTTP
      label: 端口
      rule: paramPort
      type: number
      required: true
      default: 8922
      description:
        zh: "对外暴露的端口号，默认 8922"
        en: "External port for API, default 8922"
